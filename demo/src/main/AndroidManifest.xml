<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.pisen.baselib.demo">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".SampleApplication"
        tools:replace="android:theme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ToastorActivity" />
        <activity android:name=".WifiMonitorActivity"></activity>
        <activity android:name=".FileUtilTestActivity"></activity>
        <activity android:name=".JsonPTestActivity"/>
        <activity android:name=".volley.GetActivity"></activity>
        <activity android:name=".volley.TestVolleyActivity"></activity>
        <activity android:name=".volley.PostActivity"></activity>
        <activity android:name=".volley.ResultActivity"></activity>
    </application>

</manifest>
